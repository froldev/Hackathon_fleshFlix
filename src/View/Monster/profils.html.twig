{% extends 'layout.html.twig' %}

{% block content %}
    <section id="profil">
        <h1>Ton profil :</h1>
        <h2>{{ movies.profil }}</h2>
        <p>Tu dois le sélectionner :</p>
        <div class="row">
            {% for movie in movies %}
                <div class="col-4">
                    <a href="/monster/final/">
                        <div class="card">
                            <img src="{{ movie.url }}" class="card-img-top" alt="{{ movie.title }}">
                            <div class="card-body">
                                <h3 class="card-title">{{ movie.title }}</h3>
                            </div>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}